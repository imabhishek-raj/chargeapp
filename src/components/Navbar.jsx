export default function Navbar() {
  return (
    <nav
      style={{
        padding: "15px",
        textAlign: "center",
        fontSize: "24px",
        fontWeight: "bold",
        backgroundColor: "#f5f5f5",
        borderRadius: "10px",
        marginBottom: "20px",
      }}
    >
      ⚡ ChargeApp
    </nav>
  );
}